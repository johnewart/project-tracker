<h2>Creating New Task for <%= @project.name %>...</h2>
<% form_tag :action => 'create' do %>
	<%= render :partial => 'form' %>
	<%= hidden_field 'task', 'project_id', :value => @project.id %>
  <div class="linkbar">
	<%= submit_tag "Create Task" %> 
  </div>
<% end %>